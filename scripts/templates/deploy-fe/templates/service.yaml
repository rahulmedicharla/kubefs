apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-api-deploy.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "test-api-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-api-deploy.selectorLabels" . | nindent 4 }}
