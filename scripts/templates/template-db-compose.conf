services:
  {{NAME}}:
    image: cassandra:latest
    ports:
      - "{{HOST_PORT}}:{{PORT}}"
    environment:
      - CASSANDRA_CLUSTER_NAME={{NAME}}
    networks:
      - cassandra_network
    volumes:
      - cassandra_data:/var/lib/cassandra

networks:
  cassandra_network:

volumes:
  cassandra_data: